CloudIQ Agent Instructions

Use Python 3.11 only.
Work one implementation phase at a time.
Do not modify unrelated files.
Never commit raw datasets, credentials, .env files, trained models, MLflow runs, logs, or generated Delta tables.
Use GitLab CI through .gitlab-ci.yml, not GitHub Actions.
Add type hints, docstrings, logging, and tests for new modules.
Run ruff and pytest before declaring a phase complete.
Report changed files, commands run, test results, and unresolved issues after every task.
Never invent model metrics; report only measured results.